import React from 'react';
import propTypes from 'prop-types';

const ScrollText = () => {
    return (
      <div>
        I'm text that will eventually scroll
      </div>
    )
}

export default ScrollText;